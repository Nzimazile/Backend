# This config was automatically generated from your source code
# Stacks detected: deps:node:.,deps:php:node_modules/moment-timezone,package_manager:yarn:,test:jest:
version: 2.1
orbs:
  node: circleci/node@5
jobs:
  test-node:
    # Install node dependencies and run tests
    executor: node/default
    environment:
      JEST_JUNIT_OUTPUT_DIR: ./test-results/
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: yarn
      - run:
          command: yarn add jest-junit --ignore-workspace-root-check
      - run:
          name: Run tests
          command: yarn run test --ci --runInBand --reporters=default --reporters=jest-junit
      - store_test_results:
          path: ./test-results/
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
sonarqube:
working_directory : ~/build
Docker : 
- image: sonasource/sonar-scanner-cli
resource-class: small 
steps:
- run: find  . 
- attach_workspace:
at: ~/workspace
- run: 
SONAR_BRANCH="${CIRCLE_BRANCH: -master}"
echo "Sonar branch value is: " $SONAR_BRANCH
echo "Sonar org value is: " $SONAR_ORG
sonar-scanner \ 
-Dsonar.projectKey="$SONAR_PROJECT_KEY" \
-Dsonar.organization="$SONAR_ORG" \
-Dsonar.host.url=https://sonarcloud.io \
-Dsonar.exclusions="**/android/**,**/ios/** \
-Dsonar.login="$SONAR_TOKEN" \
-Dsonar.branch.name="$SONAR_BRANCH \
-Dsonar.sources="."
          
workflows:
  build-and-test:
    jobs:
      - test-node
    # - deploy:
    #     requires:
    #       - test-node
